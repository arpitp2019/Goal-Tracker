# Legacy Static Version

This folder contains the older standalone HTML and `server.js` version of the app.

The active product is the Spring Boot + React implementation:
- `backend/` (Spring Boot)
- `frontend/` (React + Vite)

